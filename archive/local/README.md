# Historical local agent configuration

`AGENTS.md` was moved here from the repository root because it names a machine-specific Python installation and GPU rule. It is not required for public reproduction. The current portable setup is documented in the root README and `requirements.txt`.
